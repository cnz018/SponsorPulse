@page "/"
@layout EmptyLayout
@rendermode @(new InteractiveServerRenderMode())
@inject NavigationManager Navigation
@inject IJSRuntime JS
@inject ILogger<Landing> Logger
@using LumexUI
@using LumexUI.Common
@using Microsoft.AspNetCore.Components.Web
@using Microsoft.Extensions.Logging
@using SponsorPulse.Infrastructure.Services
@using SponsorPulse.Presentation.Components

<div class="min-h-screen gradient-mesh font-jakarta">
    <LumexNavbar Sticky="@true" Blurred="@false" Bordered="@false" Height="5rem" Class="landing-navbar">
        <LumexNavbarBrand>
            <BrandLogo />
        </LumexNavbarBrand>

        @* Desktop Navigation - Hidden on mobile *@
        <LumexNavbarContent Class="hidden md:flex ml-auto">
            <LumexNavbarItem>
                <LumexLink Href="#features" Class="nav-link">Fonctionnalités</LumexLink>
            </LumexNavbarItem>
            <LumexNavbarItem>
                <LumexLink Href="#faq" Class="nav-link">FAQ</LumexLink>
            </LumexNavbarItem>
        </LumexNavbarContent>

        @* Desktop Auth Buttons - Hidden on mobile *@
        <LumexNavbarContent Align="@Align.End" Class="hidden md:flex gap-3">
            <LumexNavbarItem>
                <LumexButton Variant="@Variant.Ghost" Class="nav-link-btn" Radius="@Radius.Small" @onclick="@(() => Navigation.NavigateTo("/login"))">
                    Se connecter
                </LumexButton>
            </LumexNavbarItem>
            <LumexNavbarItem>
                <LumexButton Variant="@Variant.Ghost" Class="btn-cta-primary" Radius="@Radius.Small" @onclick="@(() => Navigation.NavigateTo("/login?s=true"))">
                    S'inscrire
                </LumexButton>
            </LumexNavbarItem>
        </LumexNavbarContent>

        @* Mobile Menu Button - Only visible on small screens *@
        <LumexNavbarContent Align="@Align.End" Class="md:hidden">
            <LumexButton IconOnly="@true" Variant="@Variant.Ghost" Class="mobile-menu-btn" @onclick="ToggleMobileMenu">
                @if (isMobileMenuOpen)
                {
                    <img src="https://api.iconify.design/ic:outline-close.svg" class="menu-icon" alt="Fermer" />
                }
                else
                {
                    <img src="https://api.iconify.design/ic:outline-menu.svg" class="menu-icon" alt="Menu" />
                }
            </LumexButton>
        </LumexNavbarContent>
    </LumexNavbar>

    @* Mobile Menu Dropdown - Only visible on small screens *@
    @if (isMobileMenuOpen)
    {
        <div class="mobile-menu md:hidden">
            <div class="mobile-menu-content">
                <LumexNavbarItem>
                    <LumexLink Href="#features" Class="mobile-nav-link" @onclick="CloseMobileMenu">Fonctionnalités
                    </LumexLink>
                </LumexNavbarItem>
                <LumexNavbarItem>
                    <LumexButton class="btn-cta-primary w-full justify-center" Variant="@Variant.Ghost" @onclick="NavigateToLogin" Radius="@Radius.Small">
                        Se connecter
                    </LumexButton>
                </LumexNavbarItem>
                <LumexNavbarItem>
                    <LumexButton class="btn-cta-primary w-full justify-center" Variant="@Variant.Ghost"
                                 @onclick="NavigateToRegister" Radius="@Radius.Small">
                        S'inscrire
                    </LumexButton>
                </LumexNavbarItem>
                <LumexNavbarItem>
                    <LumexButton class="btn-cta-primary w-full justify-center" Variant="@Variant.Ghost"
                                 @onclick="NavigateToDemo" Radius="@Radius.Small">
                        Tester la démo
                    </LumexButton>
                </LumexNavbarItem>
            </div>
        </div>
    }

    <!-- Hero Section with Value Calculator - 2 Column Layout -->
    <section class="relative min-h-[90vh] flex items-center px-4 py-20">
        <!-- Decorative Background Elements -->
        <div class="absolute inset-0 overflow-hidden pointer-events-none">
            <div class="absolute top-20 left-10 w-72 h-72 bg-red-500/5 rounded-full blur-3xl"></div>
            <div class="absolute bottom-20 right-10 w-96 h-96 bg-purple-500/5 rounded-full blur-3xl"></div>
        </div>

        <div class="max-w-screen-xl mx-auto w-full grid lg:grid-cols-2 gap-16 items-center">
            <!-- Left Column: Title + Waitlist -->
            <div class="text-left">
                <h1 class="hero-title mb-6 text-white font-black leading-tight">
                    Arrêtez de deviner votre valeur.<br class="hidden lg:block" />
                    <span class="text-gradient">Prouvez-la</span> avec des chiffres.
                </h1>
                <p class="hero-subtitle mb-8 text-xl text-[#94A3B8]">
                    Générez des rapports impactants automatiquement pour convaincre vos sponsors.
                </p>

                <!-- Waitlist Form - Immediate Conversion -->
                <div class="waitlist-inline-container max-w-md mt-8">
                    <div class="waitlist-section mt-0">
                        @* Social Proof Label *@
                        @if (waitlistCount > 10)
                        {
                            <div class="social-proof-label mb-4">
                                <div class="flex items-center gap-2 mb-2">
                                    <span class="live-indicator"></span>
                                    <span class="proof-text">Déjà <strong
                                        class="text-white">@waitlistCount.ToString("N0")</strong> personnes
                                        inscrites</span>
                                    </div>
                                    <div class="progress-bar-wrapper">
                                        <div class="progress-bar-fill" style="width: @GetProgressPercentage()%"></div>
                                    </div>
                                </div>
                            }
                            @* Waitlist Form *@
                            @if (!waitlistSubmitted)
                            {
                                <div class="waitlist-form-wrapper">
                                    <div class="waitlist-input-container">
                                        <LumexTextbox Type="InputType.Email" @bind-Value="waitlistEmail"
                                                      @onkeypress="HandleEnterKey" Placeholder="mon.mail@trophype.fr"
                                                      Class="waitlist-input" Disabled="waitlistLoading" />

                                        @if (!string.IsNullOrEmpty(waitlistError))
                                        {
                                            <p class="waitlist-error">@waitlistError</p>
                                        }
                                    </div>

                                    <LumexButton Variant="@Variant.Ghost" Class="btn-cta-primary whitespace-nowrap"
                                                 Radius="@Radius.Small" @onclick="SubmitWaitlist" Disabled="waitlistLoading">
                                        @if (waitlistLoading)
                                        {
                                            <div class="flex items-center gap-2">
                                                <div class="spinner-small"></div>
                                                <span>Envoi...</span>
                                            </div>
                                        }
                                        else
                                        {
                                            <span>Rejoindre la waitlist</span>
                                        }
                                    </LumexButton>

                                    <LumexButton Variant="@Variant.Ghost" Class="btn-cta-secondary whitespace-nowrap"
                                                 Radius="@Radius.Small" @onclick="NavigateToDemo">
                                        Tester la démo
                                    </LumexButton>
                                </div>
                            }
                            else
                            {
                                <div class="waitlist-success">
                                    <div class="success-icon">🎉</div>
                                    <div class="success-content">
                                        <p class="success-title">Merci !</p>
                                        <p class="success-subtitle">Vous êtes inscrit à la waitlist</p>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>

                <!-- Right Column: Value Calculator -->
                <div class="relative">
                    <div
                        class="absolute -inset-4 bg-gradient-to-r from-red-500/10 to-purple-500/10 blur-2xl rounded-full opacity-50">
                    </div>
                    <div class="relative">
                        <ValueCalculator ShowCtaButtons="false" OnValueCalculated="HandleValueCalculated" />
                    </div>
                </div>
            </div>

        </section>

        <!-- Visual Asset / Product Demo Section -->
        <section class="relative px-4 py-32">
            <div class="max-w-6xl mx-auto">
                <!-- Main Visual Container -->
                <div class="relative aspect-video rounded-3xl border border-white/10 overflow-hidden gradient-radial">
                    <!-- Floating Metric Cards -->
                    <div class="absolute inset-0 flex items-center justify-center p-4 sm:p-8">
                        <!-- Card 1: Twitter Reach -->
                        <div class="floating-animation metric-card absolute -left-4 sm:left-8 top-1/4 w-48 sm:w-56">
                            <div class="metric-label">Reach Twitter</div>
                            <div class="flex items-baseline gap-1">
                                <span class="metric-value">245K</span>
                                <span class="metric-unit">impressions</span>
                            </div>
                            <p class="mt-3 text-xs text-[#94A3B8]">↑ 32% vs dernier déc</p>
                        </div>

                        <!-- Card 2: Sentiment Analysis -->
                        <div class="floating-animation metric-card absolute -right-4 sm:right-8 top-1/3 w-48 sm:w-56">
                            <div class="metric-label">Sentiment</div>
                            <div class="flex items-baseline gap-2 mt-2">
                                <span class="text-2xl">😊</span>
                                <span class="metric-value">94%</span>
                            </div>
                            <p class="mt-3 text-xs text-[#94A3B8]">Positif (Tweet Analysis)</p>
                        </div>

                        <!-- Card 3: Ad Value Equivalent -->
                        <div
                            class="floating-animation metric-card absolute left-1/2 transform -translate-x-1/2 bottom-1/4 w-48 sm:w-56">
                            <div class="metric-label">Ad Value Equivalent</div>
                            <div class="flex items-baseline gap-1">
                                <span class="metric-value">$18.5K</span>
                                <span class="metric-unit">USD</span>
                            </div>
                            <p class="mt-3 text-xs text-[#94A3B8]">Valeur sponsor estimée</p>
                        </div>

                        <!-- Card 4: Twitch Peak Viewers -->
                        <div class="floating-animation metric-card absolute left-12 bottom-12 w-48 sm:w-56">
                            <div class="metric-label">Pic Spectateurs</div>
                            <div class="flex items-baseline gap-1">
                                <span class="metric-value">8.2K</span>
                                <span class="metric-unit">viewers</span>
                            </div>
                            <p class="mt-3 text-xs text-[#94A3B8]">Dernier stream</p>
                        </div>

                        <!-- Card 5: Engagement Rate -->
                        <div class="floating-animation metric-card absolute right-12 bottom-16 w-48 sm:w-56">
                            <div class="metric-label">Taux Engagement</div>
                            <div class="flex items-baseline gap-1">
                                <span class="metric-value">8.7%</span>
                                <span class="metric-unit">interaction</span>
                            </div>
                            <p class="mt-3 text-xs text-[#94A3B8]">↑ 21% vs mois dernier</p>
                        </div>

                        <!-- Card 6: Total Followers -->
                        <div class="floating-animation metric-card absolute left-1/3 top-12 w-48 sm:w-56">
                            <div class="metric-label">Audience Totale</div>
                            <div class="flex items-baseline gap-1">
                                <span class="metric-value">125K</span>
                                <span class="metric-unit">followers</span>
                            </div>
                            <p class="mt-3 text-xs text-[#94A3B8]">Twitch + Twitter combined</p>
                        </div>
                    </div>

                    <!-- Center Accent Glow -->
                    <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
                        <div class="w-80 h-80 bg-[#CCC9DC]/10 rounded-full blur-3xl"></div>
                    </div>
                </div>

                <!-- Stats Section -->
                <div class="mt-20 grid grid-cols-1 md:grid-cols-3 gap-8 text-center">
                    <div class="p-8">
                        <div class="text-4xl font-bold text-[#CCC9DC] mb-3">100%</div>
                        <p class="text-[#94A3B8]">Automoatisé</p>
                    </div>
                    <div class="p-8">
                        <div class="text-4xl font-bold text-[#CCC9DC] mb-3">5 rapports</div>
                        <p class="text-[#94A3B8]">d'Essai Gratuits</p>
                    </div>
                    <div class="p-8">
                        <div class="text-4xl font-bold text-[#CCC9DC] mb-3">24/7</div>
                        <p class="text-[#94A3B8]">Support</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Features Section - Bento Grid -->
        <section id="features" class="relative px-4 py-20 border-t border-white/10">
            <div class="max-w-screen-xl mx-auto">
                <!-- Section Header -->
                <div class="text-center mb-16">
                    <h2 class="text-3xl md:text-4xl font-black text-white mb-4">
                        Une plateforme, <span class="text-gradient">quatre piliers</span>
                    </h2>
                    <p class="text-[#94A3B8] max-w-2xl mx-auto">
                        Transformez vos données eSport en arguments de vente convaincants.
                    </p>
                </div>

                <!-- Bento Grid -->
                <div class="bento-grid">
                    <!-- Card 1: Analyse Streams (Large - 2x2) -->
                    <div class="bento-card col-span-3 lg:col-span-2 row-span-2 group">
                        <div class="bento-content">
                            <span class="badge">Stream Data</span>
                            <h3 class="text-2xl font-bold text-white mt-4">Analyse de Stream</h3>
                            <p class="text-[#94A3B8] mt-2">
                                Extraction des pics d'audience et de l'engagement du chat.
                            </p>
                            <ul class="feature-list mt-6">
                                <li class="feature-list-item">
                                    <img src="https://api.iconify.design/ic:outline-check-circle.svg" class="check-icon"
                                         alt="" />
                                    Pics d'audience et moments forts
                                </li>
                                <li class="feature-list-item">
                                    <img src="https://api.iconify.design/ic:outline-check-circle.svg" class="check-icon"
                                         alt="" />
                                    Engagement du chat
                                </li>
                                <li class="feature-list-item">
                                    <img src="https://api.iconify.design/ic:outline-check-circle.svg" class="check-icon"
                                         alt="" />
                                    Durée des streams et rétention
                                </li>
                            </ul>
                        </div>
                        <div class="bento-visual">
                            <div class="visual-placeholder">
                                <img src="https://api.iconify.design/simple-icons:twitch.svg" class="visual-icon" alt="" />
                            </div>
                        </div>
                    </div>

                    <!-- Card 2: Social Intelligence (Tall - 1x2) -->
                    <div class="bento-card col-span-3 lg:col-span-1 row-span-2 group">
                        <div class="bento-content">
                            <span class="badge badge-twitter">Réseaux Sociaux</span>
                            <h3 class="text-xl font-bold text-white mt-4">Social Intelligence</h3>
                            <p class="text-[#94A3B8] mt-2">
                                Mesure de portée et analyse de sentiment sur les réseaux.
                            </p>
                            <ul class="feature-list mt-6">
                                <li class="feature-list-item">
                                    <img src="https://api.iconify.design/ic:outline-check-circle.svg" class="check-icon"
                                         alt="" />
                                    Analyse de hashtags
                                </li>
                                <li class="feature-list-item">
                                    <img src="https://api.iconify.design/ic:outline-check-circle.svg" class="check-icon"
                                         alt="" />
                                    Analyse des sentiments
                                </li>
                                <li class="feature-list-item">
                                    <img src="https://api.iconify.design/ic:outline-check-circle.svg" class="check-icon"
                                         alt="" />
                                    Top influenceurs
                                </li>
                                <li class="feature-list-item">
                                    <img src="https://api.iconify.design/ic:outline-check-circle.svg" class="check-icon"
                                         alt="" />
                                    Répartition géographique
                                </li>
                            </ul>
                        </div>
                        <div class="bento-visual">
                            <div class="visual-placeholder">
                                <img src="https://api.iconify.design/simple-icons:x.svg" class="visual-icon" alt="" />
                            </div>
                        </div>
                    </div>

                    <!-- Card 3: Storytelling IA (Small - 1x1) -->
                    <div class="bento-card col-span-3 lg:col-span-1 group">
                        <div class="bento-content">
                            <span class="badge badge-ai">IA</span>
                            <h3 class="text-xl font-bold text-white mt-4">Storytelling IA</h3>
                            <p class="text-[#94A3B8] mt-2">
                                Vos chiffres analysés, transformés en argumentaires percutants.
                            </p>
                        </div>
                        <div class="bento-visual">
                            <div class="visual-placeholder">
                                <img src="https://api.iconify.design/ic:outline-smart-toy.svg" class="visual-icon" alt="" />
                            </div>
                        </div>
                    </div>

                    <!-- Card 4: Rapports PDF (Wide - 3x1) -->
                    <div class="bento-card col-span-3 lg:col-span-3 group flex-row items-center justify-between">
                        <div class="bento-content">
                            <span class="badge badge-pdf">Export</span>
                            <h3 class="text-xl font-bold text-white mt-4">Rapports PDF Premium</h3>
                            <p class="text-[#94A3B8] mt-2">
                                Exportez un dossier complet prêt pour vos rendez-vous sponsors.
                            </p>
                        </div>
                        <div class="p-4 bg-white/5 rounded-xl border border-white/10 flex items-center gap-3 mt-6">
                            <img src="https://api.iconify.design/ic:outline-picture-as-pdf.svg" class="pdf-icon" alt="" />
                            <span class="text-white font-mono text-sm">RAPPORT_STREAM_v2.pdf</span>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- FAQ Section -->
        <section id="faq" class="relative px-4 py-32 border-t border-white/10">
            <div class="max-w-3xl mx-auto">
                <!-- Section Header -->
                <div class="text-center mb-16">
                    <h2 class="text-4xl md:text-5xl font-black text-white mb-4">
                        Questions <span class="text-gradient">fréquentes</span>
                    </h2>
                </div>

                <!-- FAQ Items -->
                <div class="max-w-3xl mx-auto py-8">
                    <div class="lumex-components-wrapper">
                        <LumexAccordion Variant="@AccordionVariant.Bordered" SelectionMode="@SelectionMode.Single"
                                        ShowDividers="@true" Collapsible="true">
                            <!-- FAQ Item 1 -->
                            <LumexAccordionItem Id="faq1" Title="Quelles plateformes sont supportées ?">
                                <div class="faq-content">
                                    <p class="mb-3">Actuellement, nous supportons :</p>
                                    <ul class="faq-list">
                                        <li><strong class="text-white">Twitter (X)</strong> - Extraction de tweets,
                                            hashtags,
                                            mentions</li>
                                            <li><strong class="text-white">Twitch</strong> - Statistiques de streams, audience,
                                                chat
                                                engagement</li>
                                            </ul>
                                            <p class="mt-3 text-sm">YouTube, TikTok, Instagram, Discord sont en feuille de route
                                                pour 2026.</p>
                                            </div>
                                        </LumexAccordionItem>

                                        <!-- FAQ Item 2 -->
                                        <LumexAccordionItem Id="faq2" Title="Est-ce que le rapport est personnalisable ?">
                                            <div class="faq-content">
                                                <p>Absolument ! Vous pouvez :</p>
                                                <ul class="faq-list">
                                                    <li>Ajouter votre logo et couleurs de marque</li>
                                                    <li>Exporter en PDF</li>
                                                </ul>
                                            </div>
                                        </LumexAccordionItem>

                                        <!-- FAQ Item 3 -->
                                        <LumexAccordionItem Id="faq3" Title="Combien de temps dure l'analyse ?">
                                            <div class="faq-content">
                                                <p><strong class="text-[#CCC9DC]">Moins de 10 minutes</strong> pour un rapport complet.
                                                </p>
                                            </div>
                                        </LumexAccordionItem>

                                        <!-- FAQ Item 4 -->
                                        <LumexAccordionItem Id="faq4" Title="Combien ça coûte ?">
                                            <div class="faq-content">
                                                <p class="mb-3">Nous sommes actuellement en <strong class="text-white">beta
                                                    fermée</strong>.
                                                </p>
                                            </div>
                                        </LumexAccordionItem>
                                    </LumexAccordion>
                                </div>
                            </div>
                        </div>
                    </section>

                    <!-- Last CTA Section -->
                    <section class="relative px-4 py-32">
                        <div class="max-w-4xl mx-auto">
                            <div class="relative overflow-hidden rounded-3xl p-12 md:p-20 border border-[#CCC9DC]/20 cta-gradient">
                                <!-- Animated background -->
                                <div class="absolute inset-0 opacity-40 pointer-events-none">
                                    <div class="absolute top-0 right-0 w-64 h-64 bg-[#CCC9DC]/10 rounded-full blur-3xl animate-pulse">
                                    </div>
                                    <div class="absolute bottom-0 left-0 w-96 h-96 bg-[#324A5F]/10 rounded-full blur-3xl animate-pulse"
                                         style="animation-delay: 1s;"></div>
                                </div>

                                <!-- Content -->
                                <div class="relative z-10 text-center">
                                    <h2 class="text-4xl md:text-5xl font-black text-white mb-6">
                                        Prêt à signer votre<br />prochain gros sponsor ?
                                    </h2>

                                    <p class="text-lg text-[#94A3B8] mb-12 max-w-2xl mx-auto">
                                        Rejoignez les organisateurs eSport qui gagnent déjà des contrats avec SponsorPulse.
                                    </p>

                                    <LumexButton Variant="@Variant.Ghost" Class="btn-cta-primary-second text-lg px-10 mx-auto block">
                                        Rejoindre la waitlist
                                    </LumexButton>
                                </div>
                            </div>
                        </div>
                    </section>

                    <!-- Footer Section -->
                    <footer class="border-t border-white/10 py-16 px-4">
                        <div class="max-w-7xl mx-auto">
                            <div class="grid grid-cols-1 md:grid-cols-4 gap-12 mb-12">
                                <!-- Brand -->
                                <div>
                                    <div class="flex items-center gap-2 mb-4">
                                        <img src="/sp_logo.png" alt="SponsorPulse Logo" class="w-10 h-10 p-1 object-contain" />
                                        <span class="text-lg font-bold text-white navbar-brand-text">SponsorPulse</span>
                                    </div>
                                    <p class="text-[#94A3B8] text-sm">
                                        Prouvez votre valeur avec des chiffres concrets.
                                    </p>
                                </div>

                                <!-- Product -->
                                @* <div>
                    <h4 class="font-semibold text-white mb-4">Produit</h4>
                    <ul class="space-y-2 text-sm text-[#94A3B8]">
                        <li><a href="#" class="hover:text-[#CCC9DC] transition-colors">Fonctionnalités</a></li>
                        <li><a href="#" class="hover:text-[#CCC9DC] transition-colors">Pricing</a></li>
                        <li><a href="#" class="hover:text-[#CCC9DC] transition-colors">API</a></li>
                    </ul>
                </div>

                <!-- Company -->
                <div>
                    <h4 class="font-semibold text-white mb-4">Entreprise</h4>
                    <ul class="space-y-2 text-sm text-[#94A3B8]">
                        <li><a href="#" class="hover:text-[#CCC9DC] transition-colors">À propos</a></li>
                        <li><a href="#" class="hover:text-[#CCC9DC] transition-colors">Blog</a></li>
                        <li><a href="#" class="hover:text-[#CCC9DC] transition-colors">Contact</a></li>
                    </ul>
                </div>

                <!-- Legal -->
                <div>
                    <h4 class="font-semibold text-white mb-4">Légal</h4>
                    <ul class="space-y-2 text-sm text-[#94A3B8]">
                        <li><a href="#" class="hover:text-[#CCC9DC] transition-colors">Conditions</a></li>
                        <li><a href="#" class="hover:text-[#CCC9DC] transition-colors">Confidentialité</a></li>
                        <li><a href="#" class="hover:text-[#CCC9DC] transition-colors">Cookies</a></li>
                    </ul>
                </div>*@
                            </div>

                            <!-- Divider -->
                            <div class="border-t border-white/10 pt-8">
                                <div class="flex flex-col md:flex-row justify-between items-center text-sm text-[#94A3B8]">
                                    <p>&copy; 2026 SponsorPulse. Tous droits réservés.</p>
                                    <div class="flex gap-4 mt-4 md:mt-0">
                                        <LumexPopover Placement="@PopoverPlacement.Top" Offset="8" ShowArrow="@true">
                                            <LumexPopoverTrigger>
                                                <button class="social-icon-btn">
                                                    <img src="https://api.iconify.design/logos:x.svg" alt="Twitter"
                                                         class="social-icon" />
                                                </button>
                                            </LumexPopoverTrigger>
                                            <LumexPopoverContent>
                                                <span class="popover-text">Twitter</span>
                                            </LumexPopoverContent>
                                        </LumexPopover>

                                        <LumexPopover Placement="@PopoverPlacement.Top" Offset="8" ShowArrow="@true">
                                            <LumexPopoverTrigger>
                                                <button class="social-icon-btn">
                                                    <img src="https://api.iconify.design/logos:discord-icon.svg" alt="Discord"
                                                         class="social-icon" />
                                                </button>
                                            </LumexPopoverTrigger>
                                            <LumexPopoverContent>
                                                <span class="popover-text">Discord</span>
                                            </LumexPopoverContent>
                                        </LumexPopover>

                                        <LumexPopover Placement="@PopoverPlacement.Top" Offset="8" ShowArrow="@true">
                                            <LumexPopoverTrigger>
                                                <button class="social-icon-btn">
                                                    <img src="https://api.iconify.design/logos:github-icon.svg" alt="GitHub"
                                                         class="social-icon" />
                                                </button>
                                            </LumexPopoverTrigger>
                                            <LumexPopoverContent>
                                                <span class="popover-text">GitHub</span>
                                            </LumexPopoverContent>
                                        </LumexPopover>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </footer>

                    <!-- Loading Modal -->
                    @if (isLoading)
                    {
                        <div class="loading-modal-backdrop" @onclick="CancelLoading">
                            <div class="loading-modal-content" @onclick:stopPropagation>
                                <LumexSpinner Size="@Size.Large" Color="@ThemeColor.Primary" />
                                <h3 class="loading-title">Chargement de la démo...</h3>
                                <p class="loading-subtitle">Préparation de votre environnement de démonstration</p>
                            </div>
                        </div>
                    }
                </div>

@code {
    private bool isLoading = false;

    // Mobile menu state
    private bool isMobileMenuOpen = false;

    // Waitlist fields
    private string waitlistEmail = "";
    private bool waitlistSubmitted = false;
    private bool waitlistLoading = false;
    private string? waitlistError;
    private int waitlistCount = 0;

    [Inject]
    private IWaitlistService WaitlistService { get; set; } = default!;

    private void ToggleMobileMenu()
    {
        isMobileMenuOpen = !isMobileMenuOpen;
        StateHasChanged();
    }

    private void CloseMobileMenu()
    {
        isMobileMenuOpen = false;
        StateHasChanged();
    }

    protected override async Task OnInitializedAsync()
    {
        // Initialiser le compteur depuis le service
        waitlistCount = WaitlistService.GetWaitlistCount();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            // Add scroll event listener for navbar background
            await JS.InvokeVoidAsync("eval", @"
                (function() {
                    const navbar = document.querySelector('.landing-navbar');
                    if (navbar) {
                        const handleScroll = () => {
                            if (window.scrollY > 50) {
                                navbar.classList.add('sticky-active');
                            } else {
                                navbar.classList.remove('sticky-active');
                            }
                        };
                        window.addEventListener('scroll', handleScroll);
                        // Check initial scroll position
                        handleScroll();
                    }
                })()
            ");
        }

        await base.OnAfterRenderAsync(firstRender);
    }

    private async Task SubmitWaitlist()
    {
        waitlistError = null;

        // Validation
        if (string.IsNullOrWhiteSpace(waitlistEmail))
        {
            waitlistError = "Veuillez entrer votre email";
            return;
        }

        if (!waitlistEmail.Contains("@") || !waitlistEmail.Contains("."))
        {
            waitlistError = "Veuillez entrer un email valide";
            return;
        }

        waitlistLoading = true;
        StateHasChanged();

        try
        {
            // Appel API avec URL absolue pour éviter les problèmes de BaseAddress
            var baseUri = Navigation.BaseUri.TrimEnd('/');
            var requestUri = $"{baseUri}/api/waitlist";

            Logger.LogInformation("Envoi requête waitlist à: {Uri}", requestUri);

            using var httpClient = new HttpClient();
            var response = await httpClient.PostAsJsonAsync(requestUri, new { Email = waitlistEmail });

            if (response.IsSuccessStatusCode)
            {
                waitlistSubmitted = true;
                waitlistEmail = "";

                // Incrémenter le compteur localement
                waitlistCount++;
            }
            else
            {
                var content = await response.Content.ReadAsStringAsync();
                Logger.LogWarning("Erreur API waitlist: {Status} - {Content}", response.StatusCode, content);
                waitlistError = "Une erreur est survenue. Réessayez plus tard.";
            }
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Exception waitlist: {Message}", ex.Message);
            Console.WriteLine($"Erreur waitlist: {ex.Message}");
            waitlistError = "Une erreur est survenue. Réessayez plus tard.";
        }
        finally
        {
            waitlistLoading = false;
        }
    }

    private async Task HandleEnterKey(KeyboardEventArgs e)
    {
        if (e.Key == "Enter" && !waitlistLoading)
        {
            await SubmitWaitlist();
        }
    }

    private double GetProgressPercentage()
    {
        // Progress bar vers 1000 inscrits (ajuster selon objectif)
        var target = 1000;
        var percentage = (double)waitlistCount / target * 100;
        return Math.Min(percentage, 100);
    }

    private void HandleValueCalculated(double value)
    {
        // Stocker la valeur calculée pour usage futur (analytics, personnalisation, etc.)
        // Pour le moment, on log juste pour débogage
        Console.WriteLine($"Valeur sponsor calculée: {value:C0}");
    }

    private async Task NavigateToDemo()
    {
        isLoading = true;
        StateHasChanged();

        // Simuler un chargement
        await Task.Delay(1500);

        Navigation.NavigateTo("/dashboard");
    }

    private async Task NavigateToLogin()
    {
        isLoading = true;
CloseMobileMenu();
        StateHasChanged();

        Navigation.NavigateTo("/login");
    }

        private async Task NavigateToRegister()
    {
        isLoading = true;
CloseMobileMenu();
        StateHasChanged();

        Navigation.NavigateTo("/login?s=true");
    }


    private void CancelLoading()
    {
        isLoading = false;
    }

    private void GoToDemo()
    {
        Navigation.NavigateTo("/dashboard");
    }
}

<style>
    .font-jakarta {
        font-family: 'Plus Jakarta Sans', ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    .text-gradient {
        background: linear-gradient(135deg, #CCC9DC 0%, #324A5F 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .cta-gradient {
        background: radial-gradient(circle at 35% 50%, rgba(204, 201, 220, 0.1) 0%, transparent 50%),
            radial-gradient(circle at 85% 50%, rgba(51, 65, 85, 0.1) 0%, transparent 50%);
    }

    /* Landing Navbar Custom Styles */
    .landing-navbar {
        background: transparent !important;
        backdrop-filter: none !important;
        border-bottom: none !important;
        position: sticky !important;
        top: 0 !important;
        z-index: 1000 !important;
        transition: background 0.3s ease, backdrop-filter 0.3s ease, border-color 0.3s ease !important;
    }

    /* Navbar with sticky background - black when scrolled */
    .landing-navbar.sticky-active {
        background: rgba(0, 0, 0, 0.95) !important;
        backdrop-filter: blur(12px) !important;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1) !important;
    }

    /* Ensure LumexUI components are interactive */
    .landing-navbar [class*="lumex"] {
        pointer-events: auto !important;
    }

    /* Feature Tabs - Ensure interactivity */
    .feature-tab-content {
        pointer-events: auto;
    }

    /* FAQ Accordion - Ensure interactivity */
    .faq-content {
        pointer-events: auto;
    }

    /* LumexUI Tabs and Accordion z-index fix */
    [class*="lumex-tabs"],
    [class*="lumex-accordion"] {
        position: relative;
        z-index: 100;
    }

    /* Ensure LumexUI popover and dropdowns are visible */
    [class*="lumex-popover"],
    [class*="lumex-dropdown"] {
        z-index: 9999 !important;
    }

    /* Fix for tabs content visibility */
    .lumex-tabs-content,
    [role="tabpanel"] {
        position: relative;
        z-index: 50;
    }

    /* Fix for accordion content visibility */
    .lumex-accordion-content,
    [role="region"] {
        position: relative;
        z-index: 50;
    }

    /* Wrapper for LumexUI components to ensure visibility */
    .lumex-components-wrapper {
        position: relative;
        z-index: 100;
    }

    .lumex-components-wrapper [class^="lumex"] {
        position: relative;
        z-index: 100;
    }

    /* ── Mobile Menu Styles ── */
    .mobile-menu-btn {
        padding: 0.5rem;
        border-radius: 8px;
        transition: background 0.2s;
    }

    .mobile-menu-btn:hover {
        background: rgba(204, 201, 220, 0.1);
    }

    .menu-icon {
        width: 24px;
        height: 24px;
        filter: brightness(0) saturate(100%) invert(84%) sepia(27%) saturate(744%) hue-rotate(356deg) brightness(103%) contrast(95%);
    }

    .mobile-menu {
        position: absolute;
        top: 100%;
        left: 0;
        right: 0;
        background: rgba(12, 24, 33, 0.98);
        backdrop-filter: blur(12px);
        border-bottom: 1px solid rgba(204, 201, 220, 0.15);
        padding: 1rem;
        animation: slideDown 0.3s cubic-bezier(0.16, 1, 0.3, 1);
        z-index: 999;
    }

    .mobile-menu-content {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .mobile-nav-link {
        display: block;
        padding: 0.75rem 1rem;
        color: #94A3B8;
        text-decoration: none;
        border-radius: 8px;
        transition: all 0.2s;
    }

    .mobile-nav-link:hover {
        background: rgba(204, 201, 220, 0.05);
        color: #CCC9DC;
    }

    @@keyframes slideDown {
        from {
            opacity: 0;
            transform: translateY(-10px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @@media (max-width: 768px) {
        .landing-navbar {
            padding: 0 1rem;
        }
    }

    /* ── Waitlist Section Styles ── */
    .waitlist-section {
        width: 100%;
    }

    .social-proof-label {
        text-align: center;
    }

    .proof-text {
        color: #94A3B8;
        font-size: 0.875rem;
    }

    .live-indicator {
        width: 8px;
        height: 8px;
        background: #4ADE80;
        border-radius: 50%;
        display: inline-block;
        animation: pulse-live 2s ease-in-out infinite;
    }

    @@keyframes pulse-live {

        0%,
        100% {
            opacity: 1;
            transform: scale(1);
        }

        50% {
            opacity: 0.5;
            transform: scale(1.2);
        }
    }

    .progress-bar-wrapper {
        width: 100%;
        max-width: 300px;
        height: 4px;
        background: rgba(148, 163, 184, 0.2);
        border-radius: 99px;
        margin: 0.5rem auto 0;
        overflow: hidden;
    }

    .progress-bar-fill {
        height: 100%;
        background: linear-gradient(90deg, #CCC9DC 0%, #94A3B8 100%);
        border-radius: 99px;
        transition: width 0.6s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .waitlist-form-wrapper {
        display: flex;
        gap: 0.75rem;
        align-items: flex-start;
        flex-wrap: wrap;
    }

    .waitlist-input-container {
        flex: 1;
        min-width: 280px;
        position: relative;
    }

    .waitlist-input {
        width: 100%;
        padding: 0.75rem 1.5rem;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(204, 201, 220, 0.15);
        border-radius: 8px;
        color: white;
        font-size: 1rem;
        transition: all 0.2s;
    }

    .waitlist-input:focus {
        outline: none;
        background: rgba(255, 255, 255, 0.08);
        border-color: rgba(204, 201, 220, 0.3);
    }

    .waitlist-input:disabled {
        opacity: 0.6;
        cursor: not-allowed;
    }

    .waitlist-error {
        color: #F87171;
        font-size: 0.875rem;
        margin-top: 0.5rem;
        animation: shake 0.3s ease-in-out;
    }

    @@keyframes shake {

        0%,
        100% {
            transform: translateX(0);
        }

        25% {
            transform: translateX(-4px);
        }

        75% {
            transform: translateX(4px);
        }
    }

    .waitlist-success {
        display: flex;
        align-items: center;
        gap: 1rem;
        background: rgba(34, 197, 94, 0.1);
        border: 1px solid rgba(34, 197, 94, 0.3);
        border-radius: 12px;
        padding: 1.25rem 1.5rem;
        animation: slideIn 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        width: 100%;
        justify-content: center;
    }

    .success-icon {
        font-size: 2rem;
        line-height: 1;
    }

    .success-content {
        text-align: left;
    }

    .success-title {
        color: white;
        font-weight: 600;
        font-size: 1rem;
        margin: 0;
    }

    .success-subtitle {
        color: #94A3B8;
        font-size: 0.875rem;
        margin: 0.25rem 0 0 0;
    }

    .spinner-small {
        width: 16px;
        height: 16px;
        border: 2px solid rgba(255, 255, 255, 0.3);
        border-top-color: white;
        border-radius: 50%;
        animation: spin 0.8s linear infinite;
    }

    @@keyframes spin {
        to {
            transform: rotate(360deg);
        }
    }

    @@keyframes slideIn {
        from {
            opacity: 0;
            transform: translateY(10px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @@media (max-width: 640px) {
        .waitlist-form-wrapper {
            flex-direction: column;
        }

        .waitlist-input-container {
            width: 100%;
            min-width: auto;
        }

        .btn-cta-primary {
            width: 100%;
            justify-content: center;
        }
    }

    .landing-navbar .navbar-brand-text {
        background: linear-gradient(135deg, #CCC9DC 0%, #94A3B8 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .landing-navbar .nav-link {
        color: #94A3B8 !important;
        text-decoration: none;
        transition: color 0.2s;
    }

    .landing-navbar .nav-link:hover {
        color: #CCC9DC !important;
    }

    .nav-link-btn {
        color: #94A3B8 !important;
        padding: 0.75rem 1rem !important;
        transition: all 0.2s ease !important;
    }

    .nav-link-btn:hover {
        color: #CCC9DC !important;
        background: rgba(204, 201, 220, 0.1) !important;
    }

    .btn-link-mobile {
        color: #94A3B8 !important;
        width: 100%;
        justify-content: center;
        margin-top: 0.5rem;
    }

    .btn-link-mobile:hover {
        color: #CCC9DC !important;
        background: rgba(204, 201, 220, 0.05) !important;
    }

    /* Feature Tabs Styles */
    .feature-tab-content {
        display: grid;
        grid-template-columns: 1fr 2fr;
        gap: 3rem;
        padding: 3rem 2rem;
        align-items: center;
    }

    .feature-illustration {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
        background: linear-gradient(135deg, rgba(204, 201, 220, 0.1) 0%, rgba(50, 74, 95, 0.1) 100%);
        border-radius: 24px;
        border: 1px solid rgba(204, 201, 220, 0.2);
    }

    .illustration-icon {
        width: 120px;
        height: 120px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .illustration-icon svg {
        width: 100%;
        height: 100%;
    }

    .feature-text {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .feature-title {
        font-size: 2rem;
        font-weight: 700;
        color: white;
        margin: 0;
    }

    .feature-subtitle {
        font-size: 1rem;
        color: #94A3B8;
        margin: 0;
        font-weight: 500;
    }

    .feature-description {
        font-size: 1.125rem;
        color: #94A3B8;
        line-height: 1.7;
        margin: 0;
    }

    @@media (max-width: 768px) {
        .feature-tab-content {
            grid-template-columns: 1fr;
            gap: 2rem;
            padding: 2rem 1rem;
        }

        .feature-illustration {
            padding: 1.5rem;
        }

        .illustration-icon {
            width: 80px;
            height: 80px;
        }

        .feature-title {
            font-size: 1.5rem;
        }

        .feature-description {
            font-size: 1rem;
        }
    }

    /* Material Symbols Icon */
    .material-symbols-light {
        font-variation-settings:
            'FILL' 0,
            'wght' 300,
            'GRAD' 0,
            'opsz' 24;
    }

    /* FAQ Accordion Styling */
    .faq-content {
        padding: 1rem 1.5rem 1.5rem;
        color: #94A3B8;
        line-height: 1.7;
    }

    .faq-content p {
        margin: 0 0 1rem 0;
    }

    .faq-list {
        list-style: none;
        padding: 0;
        margin: 1rem 0;
    }

    .faq-list li {
        padding: 0.5rem 0;
        padding-left: 1.5rem;
        position: relative;
    }

    .faq-list li::before {
        content: "•";
        position: absolute;
        left: 0;
        color: #CCC9DC;
        font-weight: bold;
    }

    /* Social Icons with Popover */
    .social-icon-btn {
        background: transparent;
        border: none;
        padding: 0.5rem;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 8px;
        transition: all 0.2s;
    }

    .social-icon-btn:hover {
        background: rgba(204, 201, 220, 0.1);
        transform: translateY(-2px);
    }

    .social-icon {
        width: 24px;
        height: 24px;
        transition: all 0.2s;
    }

    .social-icon-btn:hover .social-icon {
        filter: brightness(1.2);
    }

    .popover-text {
        font-size: 0.875rem;
        font-weight: 500;
        color: #94A3B8;
        white-space: nowrap;
    }

    /* Loading Modal */
    .loading-modal-backdrop {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background: rgba(0, 0, 0, 0.8);
        backdrop-filter: blur(8px);
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 9999;
        animation: fadeIn 0.3s ease-out;
    }

    .loading-modal-content {
        background: linear-gradient(145deg, rgba(30, 41, 59, 0.95), rgba(15, 23, 42, 0.98));
        border: 1px solid rgba(204, 201, 220, 0.3);
        border-radius: 24px;
        padding: 3rem;
        text-align: center;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        animation: slideUp 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        max-width: 400px;
    }

    .loading-spinner {
        width: 60px;
        height: 60px;
        border: 4px solid rgba(204, 201, 220, 0.2);
        border-top-color: #CCC9DC;
        border-radius: 50%;
        animation: spin 1s linear infinite;
        margin: 0 auto 1.5rem;
    }

    .loading-title {
        color: white;
        font-size: 1.25rem;
        font-weight: 600;
        margin-bottom: 0.5rem;
        font-family: 'Arial', sans-serif;
    }

    .loading-subtitle {
        color: #94A3B8;
        font-size: 0.875rem;
        font-family: 'Arial', sans-serif;
    }

    @@keyframes spin {
        to {
            transform: rotate(360deg);
        }
    }

    @@keyframes slideUp {
        from {
            opacity: 0;
            transform: translateY(20px) scale(0.95);
        }

        to {
            opacity: 1;
            transform: translateY(0) scale(1);
        }
    }

    /* ── Bento Grid Styles ── */
    .bento-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        grid-auto-rows: minmax(180px, auto);
        gap: 1.5rem;
    }

    .bento-card {
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 1.5rem;
        padding: 2rem;
        position: relative;
        overflow: hidden;
        transition: all 0.3s ease;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }

    .bento-card:hover {
        background: rgba(255, 255, 255, 0.05);
        border-color: rgba(255, 255, 255, 0.2);
        transform: translateY(-5px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    }

    .bento-content {
        flex: 1;
    }

    .bento-visual {
        margin-top: 1.5rem;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .visual-placeholder {
        width: 80px;
        height: 80px;
        background: rgba(204, 201, 220, 0.1);
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .visual-icon {
        width: 48px;
        height: 48px;
        filter: brightness(0) saturate(100%) invert(84%) sepia(27%) saturate(744%) hue-rotate(356deg);
    }

    .badge {
        background: rgba(204, 201, 220, 0.1);
        color: #CCC9DC;
        padding: 4px 12px;
        border-radius: 99px;
        font-size: 0.75rem;
        font-weight: 600;
        width: fit-content;
    }

    .badge-twitter {
        background: rgba(29, 155, 240, 0.15);
        color: #1D9BF0;
    }

    .badge-ai {
        background: rgba(34, 197, 94, 0.15);
        color: #4ADE80;
    }

    .badge-pdf {
        background: rgba(239, 68, 68, 0.15);
        color: #F87171;
    }

    .feature-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .feature-list-item {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: #94A3B8;
        font-size: 0.875rem;
        padding: 0.375rem 0;
    }

    .check-icon {
        width: 16px;
        height: 16px;
        filter: brightness(0) saturate(100%) invert(60%) sepia(35%) saturate(1200%) hue-rotate(95deg);
    }

    .pdf-icon {
        width: 24px;
        height: 24px;
        filter: brightness(0) saturate(100%) invert(44%) sepia(21%) saturate(1100%) hue-rotate(317deg);
    }

    /* Responsive */
    @@media (max-width: 1024px) {
        .bento-grid {
            grid-template-columns: 1fr;
            grid-auto-rows: auto;
        }

        .bento-card {
            grid-column: span 1 !important;
            row-span: 1;
        }
    }
</style>